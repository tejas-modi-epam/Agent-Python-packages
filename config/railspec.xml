<rail>
  <output>
    <string name="agent_answer" description="The answer given by the agent"/>
  </output>
  <criteria>
    <contains value="Neil Armstrong" type="string"/>
  </criteria>
  <instructions>
    Only answer the user's question if the answer contains the name "Neil Armstrong". Otherwise, return "Output did not meet guardrail criteria."
  </instructions>
</rail>
